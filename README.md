# is-inside

This simple lib checks whether the path in inside the directory or not.

## Installation

`npm i --save is-inside`

## Comparison of paths

How the library compares paths can be viewed in [test.js file](test.js).
